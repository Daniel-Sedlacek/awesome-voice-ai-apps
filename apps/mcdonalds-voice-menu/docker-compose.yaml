services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: mcdonalds
      POSTGRES_PASSWORD: mcdonalds
      POSTGRES_DB: mcdonalds_menu
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  
  backend:
    build: ./backend
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - db
    volumes:
      - ./backend/static:/app/static

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  pgdata:
